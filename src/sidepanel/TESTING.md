# Side Panel Testing Guide

## 功能概览

Side Panel 翻译抽屉UI已完成，所有样式都使用Tailwind CSS实现，仅保留必要的自定义CSS（滚动条样式）。

## 已实现的功能

### ✅ 核心组件
- `sidepanel.html` - HTML入口文件
- `index.tsx` - React初始化
- `App.tsx` - 主应用容器，处理消息通信
- `TranslationView.tsx` - 翻译UI主组件（300+行）
- `sidepanel.css` - 最小化自定义CSS（仅滚动条）
- `README.md` - 详细文档

### ✅ UI功能
1. **空状态** - 欢迎界面，带使用提示
2. **加载状态** - 带旋转动画的加载器
3. **错误状态** - 友好的错误提示和重试按钮
4. **翻译展示**：
   - 原文卡片（带发音按钮）
   - 中文翻译卡片（带复制按钮）
   - 详细解释列表
   - 例句展示
   - 网络翻译

### ✅ 交互功能
- 🔊 音频发音（Web Speech API + Youdao TTS备用）
- 📋 一键复制翻译
- ⭐ 添加到生词本（自动检测已保存）
- 🎨 深色模式支持

### ✅ 样式架构
- **Tailwind CSS**: 所有布局和样式都使用Tailwind工具类
- **自定义动画**: 在`tailwind.config.js`中定义
  - `animate-slide-in`: 卡片滑入动画
  - `animate-audio-pulse`: 音频播放脉冲动画
- **自定义CSS**: 仅用于WebKit滚动条样式（不可用Tailwind实现）

## Tailwind配置

### 新增的自定义动画
```javascript
// tailwind.config.js
keyframes: {
  slideIn: {
    '0%': { opacity: '0', transform: 'translateY(10px)' },
    '100%': { opacity: '1', transform: 'translateY(0)' },
  },
  audioPulse: {
    '0%, 100%': { opacity: '1' },
    '50%': { opacity: '0.6' },
  },
},
animation: {
  'slide-in': 'slideIn 0.3s ease-out',
  'audio-pulse': 'audioPulse 1s ease-in-out infinite',
}
```

### 使用方式
```tsx
// 卡片滑入动画
<div className="animate-slide-in bg-white ...">

// 音频播放动画
<button className={isPlayingAudio ? 'animate-audio-pulse' : ''}>
```

## 测试场景

### 1. 基本显示测试
- [ ] 打开side panel，看到空状态欢迎界面
- [ ] 检查深色模式切换正常
- [ ] 检查响应式布局

### 2. 翻译功能测试
- [ ] 选择单词，side panel显示翻译
- [ ] 选择短语，正确显示翻译
- [ ] 选择句子，正确显示翻译
- [ ] 检查所有卡片是否有滑入动画

### 3. 交互功能测试
- [ ] 点击发音按钮，听到正确发音
- [ ] 播放时按钮显示脉冲动画
- [ ] 点击复制按钮，成功复制翻译
- [ ] 复制后图标变为勾选标记（2秒后恢复）
- [ ] 点击"添加到生词本"，成功保存
- [ ] 已保存的词显示"已添加"状态

### 4. 错误处理测试
- [ ] 网络错误时显示错误状态
- [ ] 点击"重试"按钮可刷新
- [ ] API错误时显示友好提示

### 5. 滚动测试
- [ ] 长内容时滚动条样式正确
- [ ] 深色模式下滚动条颜色正确
- [ ] 滚动平滑无卡顿

## 样式优化说明

### 从自定义CSS迁移到Tailwind

**之前的问题**：
- 混用自定义CSS类和Tailwind类
- 动画定义在CSS文件中
- 布局样式分散在CSS和JSX中

**现在的优化**：
✅ 所有布局和颜色都用Tailwind类
✅ 动画在Tailwind配置中定义
✅ CSS文件只保留滚动条样式（Tailwind无法实现）
✅ 更好的可维护性和一致性

### CSS文件内容
仅保留28行CSS，用于自定义滚动条：
- `.sidepanel-scrollable::-webkit-scrollbar-*` 系列样式
- 支持浅色和深色模式

## 与其他模块的集成

### 消息通信
```typescript
// 从background接收翻译结果
MessageType.TRANSLATION_RESULT -> 更新state
MessageType.TRANSLATION_ERROR -> 显示错误
```

### 数据存储
```typescript
// 使用vocabularyRepository
- exists(): 检查词是否已保存
- addWord(): 添加到生词本
```

### Chrome APIs
```typescript
// chrome.runtime.onMessage - 监听消息
// chrome.storage.local - 持久化待处理翻译
// speechSynthesis - Web TTS API
```

## 下一步开发建议

1. **Content Script**: 实现文本选择监听
2. **Background Service**: 实现翻译API调用
3. **测试**: 在真实网页上测试选词翻译
4. **优化**: 根据实际使用调整UI细节

## 文件结构总结

```
src/sidepanel/
├── sidepanel.html       # 入口HTML (7行)
├── index.tsx           # React初始化 (13行)
├── App.tsx            # 应用容器 (51行)
├── TranslationView.tsx # 核心UI组件 (330行)
├── sidepanel.css      # 最小化CSS (28行)
└── README.md          # 详细文档
```

总计：~430行代码实现完整的翻译UI
